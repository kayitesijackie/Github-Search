export class User {
    constructor(public login: string, public html_url: string, public public_repos:number,public dateCount:Date, public avatar_url: string, public followers: number, public following: number) {
    }
  
}